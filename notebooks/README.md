# Notebooks

The source projects did not include Jupyter notebooks to migrate into this split repo.

Use the `examples/` and `experiments/` directories as the executable equivalents for the planned notebook walkthroughs.
